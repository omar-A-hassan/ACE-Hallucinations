# ACE Framework requirements for M3 MacBook training

# Core ACE dependencies
pydantic>=2.0.0
python-dotenv>=1.0.0
tenacity>=8.0.0

# LLM providers
litellm>=1.0.0

# MLX for Apple Silicon (M3 optimization)
mlx>=0.0.8
mlx-lm>=0.0.8

# Optional: Observability
# opik>=0.1.0  # Uncomment if using Opik tracking
